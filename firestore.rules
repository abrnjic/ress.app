rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only allow the specific admin email to read/write any document
    match /{document=**} {
      allow read, write: if request.auth != null && request.auth.token.email == "abrnjic@gmail.com";
    }
  }
}
